export interface LoginResponse {
  success?: string;
  data?: any;
  token?:string;
  // token?: string;
  // refreshtoken?: string;
  // tokenexpiration?: number;
  // startTime?: number;
}
